export type TextsType = {
  navbarMenu: {
    home: string;
    login: string;
  };
  user: {
    name: string;
    firstName: string;
    email: string;
    password: string;
  };
  formErrors: {
    requiredField: string;
  };
  landing: {
    welcome: string,
    heading: string,
    connectText: string,
    contactBtn: string,
  }, 
  footer: {
    madeWithText: string;
    inText: string;
    cityText: string;
  };
};
